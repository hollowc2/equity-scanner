SCHWAB_GATEWAY_URL=https://gateway.internal
SCHWAB_GATEWAY_API_KEY=change-me

# Optional — news providers degrade gracefully (skip that provider) when unset.
SEC_USER_AGENT=your-name-or-org contact@example.com
ALPHA_VANTAGE_API_KEY=change-me

# Optional — required only for a live (non --dry-run) equity-scanner-run.
# Separate from any webhook ButterflyGuy uses.
EQUITY_SCANNER_DISCORD_WEBHOOK_URL=change-me
